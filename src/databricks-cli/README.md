# Databricks CLI (databricks-cli)

A dev container Feature that installs the Databricks CLI using the official Databricks setup script.

## Example Usage

```jsonc
"features": {
    "ghcr.io/kingbain/devcontainer-features-databricks/databricks-cli:0.0.1": {}
}
```

## Options

This feature does not define any configurable options.